# SPORT KING

По любым вопросам и поддержке сайта ; Вацап - 87773317550, Телеграм - @CryptoJefe
## Стек

- React + Vite + TypeScript
- Tailwind CSS v4
- React Router
- Lucide React

## Запуск На локал хосте (У себя на пк)

```bash
npm install
npm run dev
```

Сайт откроется на `http://localhost:5173`

## Сборка

```bash
npm run build
npm run preview
```

## Структура

- `src/pages/` — страницы сайта
- `src/components/` — UI-компоненты, layout, секции
- `src/data/` — товары, категории, настройки сайта
- `public/` — логотип и изображения

## Покупка

Все товары ведут на Kaspi через кнопку **«КУПИТЬ НА KASPI»**.

Ссылки на товары настраиваются в `src/data/products.ts`.
В админке можно добавлять отзывы вручную? менять\добавлять\редактировать категории и товары а также их фото карточки 


Статистика сайта ө показывает количество пользователей посетивших сайт вообщем за все время,а также по дням на графике - навести мышку на столбец графика - покажет количество посещений
В статистике показано количество кликов на контакты,количество переходов на каспи

Домен будет куплен на год на Hoster.kz
Сервер нужно оплачивать ежемесячно на Render или привязать карту для автоматических списаний по тарифу






## Админ-панель

URL: секретный путь из `VITE_ADMIN_BASE` / `ADMIN_PATH` (см. `.env`), по умолчанию `/sk-manage-kz8m2p/login`. Путь `/admin` отключён.

- Пароль: переменная `ADMIN_PASSWORD` (локально в `.env`, на Render — в Environment)
- Добавление и редактирование товаров, категорий и отзывов
- Данные и загрузки: `server/data/` (`store.json`, `uploads/`)
- После неправильного ввода пародля в админке 5 раз включаеться блокировка на 15минут - во избежание перебора паролей -Rate limit

## Зазгрузка фото категориям и товарам ө советую использовать загрузку путем вставки ссылок ө это меньше будет нагружать сервер при  большом количестве каталога

